import 'package:flutter/material.dart';

class AddInstitutionScreen extends StatelessWidget {
  const AddInstitutionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Center(
          child: const Text('Add Institution Screen'),
        ),
      ),
    );
  }
}